zuul:
  ignored-services: '*'
  prefix: /api
  routes:
    organization-service: /organization/**
    licensing-service: /licensing/**

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 8000

ribbon:
  ConnectTimeout: 1000
  ReadTimeout: 3000
