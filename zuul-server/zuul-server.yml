zuul:
  ignored-services: '*'
  prefix: /api
  routes:
    organization-service: /organization/**
    licensing-service: /licensing/**

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 5000

ribbon:
  connectTimeout: 1000
  readTimeout: 3000
